# OpenAI API Key (Required for LLM service)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: If you want to use a different base URL (e.g. for DeepSeek or proxy)
# OPENAI_API_BASE=https://api.deepseek.com/v1

# Optional: Database URL (default is sqlite)
# DATABASE_URL=sqlite:///./qsnap.db
